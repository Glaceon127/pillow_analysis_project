"""本地配置示例（不要提交真实 key/token）。

用法：
1) 复制本文件为 `config/local_settings.py`
2) 填入你自己的 key/token（仅保存在本机）

注意：
- `config/local_settings.py` 应加入 .gitignore，避免误提交。
- 更推荐用环境变量：NVD_API_KEY / GITHUB_TOKEN / REQUESTS_CA_BUNDLE。
"""

# 数据库路径（可选）
# DB_PATH = r'../data/processed/pillow_analysis.db'

# NVD API Key（可选）
# API_KEY = '你的NVD_API_KEY'

# GitHub Token（可选）
# GITHUB_TOKEN = '你的GITHUB_TOKEN'

# 代理（可选）
# HTTP_PROXY = 'http://127.0.0.1:7890'
# HTTPS_PROXY = 'http://127.0.0.1:7890'

# 代理根证书 PEM（可选，解决 CERTIFICATE_VERIFY_FAILED）
# CA_BUNDLE = r'F:\\path\\to\\your-ca.pem'

# 请求头（可选）
# USER_AGENT = 'pillow-analysis/1.0'

# 启用数据源（可选）
# ENABLE_SOURCES = 'osv,nvd_v2,nvd_v1,circl,github'

# Pillow 本地 git 仓库路径（用于 tools/run_link_commits.py 默认值）
# PILLOW_REPO_PATH = r'F:\\path\\to\\Pillow'

# GitHub 仓库（用于抓取 bug issues），格式："owner/repo"
# GITHUB_REPO = 'python-pillow/Pillow'
